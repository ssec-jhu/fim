name: fim_env
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python>=3.11
  - numpy
  - scipy
  - scikit-image
  - tqdm
  - pytorch::pytorch
  # - pytorch::cpuonly   # uncomment to force CPU-only PyTorch (remove for GPU/CUDA support)
  - fastapi
  - uvicorn
